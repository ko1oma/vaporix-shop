/*
 * Legacy compatibility file.
 * Checkout step 1 now renders its single back button directly from
 * order_flow_runtime.js. This file intentionally does nothing so an
 * existing script tag cannot create a second button.
 */
(function(){
  'use strict';
})();
